{
  "name": "xp-raw-browser",
  "version": "1.0.0",
  "description": "Browser expiriments (only uses Node for serving static content)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Eschbach",
  "license": "MIT",
  "dependencies": {
    "static-server": "^3.0.0"
  }
}
